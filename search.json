[{"title":"文件操作相关函数详解","url":"/2024/03/05/文件操作函数/","content":"\n\n\n\n\n c语言文件按编码方式分为二进制文件和ASCII文件。 c语言用文件指针标识文件，对文件进行操作时，需要遵从打开(创建)->读写->关闭的流程。\n# fopen函数\n\n在C语言中，`fopen` 函数用于打开一个文件。这个函数需要两个参数：文件名和打开模式。如果打开成功，`fopen` 函数返回一个 `FILE *` 指针，这个指针可以用于后续的文件操作；如果打开失败，它返回 `NULL`。其函数原型如下所示。\n\n`FILE *fopen(char *filename, char *mode);`\n\nmode即文件打开的方式，由两类字符构成：一类是打开文件的类型，包括文本文件(**t**ext)和二进制文件(**b**inary)，如果不指定文件的类型，则默认为文本文件；另一类是操作类型，read、write、append。\n\n以下是一些常见的打开模式：\n\n- `\"r\"`：以读取模式打开文件。文件必须存在。\n- `\"w\"`：以写入模式打开文件。如果文件不存在，创建新文件；如果文件存在，删除文件内容，进行覆盖。\n- `\"a\"`：以追加模式打开文件。如果文件不存在，创建新文件；如果文件存在，将数据追加到文件末尾。\n- `\"r+\"`：以读取/写入模式打开文件。文件必须存在。\n- `\"w+\"`：以读取/写入模式打开文件。如果文件不存在，创建新文件；如果文件存在，删除文件内容，进行覆盖。\n- `\"a+\"`：以读取/追加模式打开文件。如果文件不存在，创建新文件；如果文件存在，将数据追加到文件末尾。\n- `\"wb+\"`：以读取/写入模式打开二进制文件。如果文件不存在，创建新文件；如果文件存在，删除文件内容，进行覆盖。\n\n以下是一个使用 `fopen` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    // 在这里进行文件操作\n    fclose(fp);\n    return 0;\n}\n```\n\n在这个例子中，`fopen` 函数尝试以读取模式打开 `file.txt` 文件。如果打开失败，程序打印一条错误消息并返回 `1`；如果打开成功，程序进行文件操作，然后使用 `fclose` 函数关闭文件。\n\n\n\n\n# fclose函数\n\n在C语言中，`fclose` 函数用于关闭一个打开的文件。这个函数的参数是一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。**如果关闭成功，`fclose` 函数返回 `0`；如果发生错误，它返回 `EOF`。**\n\n关闭文件是很重要的，因为它可以释放系统资源，确保所有的输出都被写入到文件中，并且可以防止文件被意外修改或删除。\n\n以下是一个使用 `fclose` 的例子：\n\n```c\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    // 在这里进行文件操作\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数打开一个文件。然后，它进行文件操作。最后，它使用 `fclose` 函数关闭文件。如果关闭失败，程序打印一条错误消息并返回 `1`。\n\n\n\n# 字符读写函数\n\n## fgetc函数\n\n在C语言中，`fgetc` 函数用于从一个**打开的文件**中读取一个字符。这个函数的参数是一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果读取成功，`fgetc` 函数**返回读取的字符**，同时将读写位置指针指向下一个字符；如果读取失败或到达文件末尾，它返回 `EOF`。\n\n以下是一个使用 `fgetc` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    int c;\n    while ((c = fgetc(fp)) != EOF) {\n        putchar(c);\n    }\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数打开一个文件。然后，它使用 `fgetc` 函数在一个循环中读取文件的每一个字符，直到到达文件末尾。每读取一个字符，程序就使用 `putchar` 函数将这个字符写入到标准输出。最后，程序使用 `fclose` 函数关闭文件。\n\n\n\n\n## fputc函数\n\n在C语言中，`fputc` 函数用于向一个打开的文件中写入一个字符。这个函数需要两个参数：要写入的字符和一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果写入成功，`fputc` 函数返回写入的字符；如果发生错误，它返回 `EOF`。其函数原型为`int fputc(char ch,FILE *fp)`\n\n以下是一个使用 `fputc` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"w\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    if (fputc('A', fp) == EOF) {\n        printf(\"Failed to write to file\\n\");\n        return 1;\n    }\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数以写入模式打开一个文件。然后，它使用 `fputc` 函数向文件中写入一个字符 'A'。如果写入失败，程序打印一条错误消息并返回 `1`。最后，程序使用 `fclose` 函数关闭文件。\n\n# 字符串读写函数\n## fgets函数\n\n在C语言中，`fgets`函数用于从指定的流中读取一行文本，直到遇到**换行符、EOF**（文件结束标志）或者读取了指定数量的字符，读取成功后返回指向字符串的指针，读到文件末尾或者出错，则返回NULL。\n**注意：** fgets函数最多读取n-1个字符长度的字符串，读取完后会在字符串的末尾加上串结束标志`'\\0'`。\n\n它的原型如下：\n```c\nchar *fgets(char *str, int n, FILE *stream);\n```\n\n`fgets`函数接受三个参数：\n\n1. **str**：这是一个字符指针，指向一个字符数组。`fgets`函数将读取的文本存储在这个数组中。\n2. **n**：这是一个整数，指定要读取的最大字符数（包括最后的空字符）。通常，这个值应该等于数组的大小。\n3. **stream**：这是一个文件指针，指向要读取的流。通常，这个值可以是`stdin`（标准输入），也可以是`fopen`函数返回的文件指针。\n\n例如，下面的代码使用`fgets`函数从标准输入读取一行文本：\n\n```c\nchar str[100];\nfgets(str, sizeof(str), stdin);\n```\n\n在这个例子中，`fgets`函数将最多读取99个字符（最后一个字符是`\\0`）并存储在`str`数组中。如果在读取99个字符之前遇到换行符或EOF，`fgets`函数将停止读取。\n\n`fgets`函数返回的是一个指向`str`的指针，如果成功的话。如果发生错误或者达到文件末尾，它将返回NULL。\n\n\n## fputs函数\n\n在C语言中，`fputs` 函数用于向一个打开的文件中写入一个字符串。这个函数需要两个参数：要写入的字符串和一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果写入成功，`fputs` 函数返回非负值；如果发生错误，它返回 `EOF`。\n\n**注意**：fputs函数不会将字符串末尾的`\\0`写入文件，也不会自动向文件中写入换行符，如\n       果需要写入一行文本，字符串需要包含`\\n`。\n\n以下是一个使用 `fputs` 的例子：\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"w\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    if (fputs(\"Hello, world!\\n\", fp) == EOF) {\n        printf(\"Failed to write to file\\n\");\n        return 1;\n    }\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数以写入模式打开一个文件。然后，它使用 `fputs` 函数向文件中写入一个字符串 \"Hello, world!\\n\"。如果写入失败，程序打印一条错误消息并返回 `1`。最后，程序使用 `fclose` 函数关闭文件。\n\n\n\n# 数据块读写函数\n\n## fread函数\n\n在C语言中，`fread` 函数用于从一个打开的文件中读取数据。这个函数需要四个参数：一个指向存储数据的缓冲区的指针，元素的大小，元素的数量，以及一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果读取成功，`fread` 函数返回实际读取的元素数量，注意不是字节个数；如果读取失败或到达文件末尾，它返回的数量可能小于请求的数量。其函数原型如下：\n`unsigned fread(void *ptr,unsigned size,unsigned n,FILE *fp);`\n\n以下是一个使用 `fread` 的例子：\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    char buffer[100];\n    size_t n = fread(buffer, sizeof(char), sizeof(buffer), fp);\n    if (ferror(fp)) {\n        printf(\"Failed to read file\\n\");\n        return 1;\n    }\n    buffer[n] = '\\0';  // 添加空字符\n    printf(\"Read %zu characters: %s\\n\", n, buffer);\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数打开一个文件。然后，它使用 `fread` 函数读取文件的内容到一个缓冲区。如果读取失败，程序打印一条错误消息并返回 `1`。然后，程序添加一个空字符到缓冲区的末尾，并使用 `printf` 函数打印读取的字符。最后，程序使用 `fclose` 函数关闭文件。\n\n\n## fwrite函数\n\n在C语言中，`fwrite` 函数用于向一个打开的文件中写入数据。这个函数需要四个参数：一个指向要写入数据的缓冲区的指针，元素的大小，元素的数量，以及一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果写入成功，`fwrite` 函数返回实际写入的元素数量；如果发生错误，它返回的数量可能小于请求的数量。\n\n以下是一个使用 `fwrite` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"w\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    char buffer[] = \"Hello, world!\";\n    size_t n = fwrite(buffer, sizeof(char), sizeof(buffer) - 1, fp);\n    if (n < sizeof(buffer) - 1) {\n        printf(\"Failed to write to file\\n\");\n        return 1;\n    }\n    printf(\"Wrote %zu characters\\n\", n);//这里打印的是size_t类型，使用%zu格式说明符，而不是%d或%u\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数以写入模式打开一个文件。然后，它使用 `fwrite` 函数将一个缓冲区的内容写入到文件。如果写入的字符数量小于缓冲区的大小，程序打印一条错误消息并返回 `1`。然后，程序使用 `printf` 函数打印写入的字符数量。最后，程序使用 `fclose` 函数关闭文件。\n\n\n# 格式化读写函数\n\n\n## fscanf函数\n\n在C语言中，`fscanf` 函数用于从一个打开的文件中读取格式化的输入。这个函数需要至少两个参数：一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的，以及一个格式字符串，这个字符串描述了要读取的数据的格式。`fscanf` 函数还可以接受额外的参数，这些参数是指向变量的指针，这些变量将存储读取的数据。\n\n如果读取成功，`fscanf` 函数返回实际读取的数据项的数量；如果到达文件末尾或发生错误，它返回 `EOF`。\n\n以下是一个使用 `fscanf` 的例子：\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    int x;\n    if (fscanf(fp, \"%d\", &x) != 1) {\n        printf(\"Failed to read integer from file\\n\");\n        return 1;\n    }\n    printf(\"Read integer: %d\\n\", x);\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数打开一个文件。然后，它使用 `fscanf` 函数从文件中读取一个整数。如果读取失败，程序打印一条错误消息并返回 `1`。然后，程序使用 `printf` 函数打印读取的整数。最后，程序使用 `fclose` 函数关闭文件。\n\n---\n## fprintf函数\n\n在C语言中，`fprintf` 函数用于向一个打开的文件中写入格式化的输出。这个函数需要至少两个参数：一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的，以及一个格式字符串，这个字符串描述了要写入的数据的格式。`fprintf` 函数还可以接受额外的参数，这些参数是要写入的数据。\n如果写入成功，`fprintf` 函数返回写入的字符数量。如果发生错误，返回EOF。\n\n**注意**：fprintf函数总是以字符串的形式将数据信息存放到文件中，而不是以数值的形式，不管打开的是文本文件还是二进制文件。\n\n以下是一个使用 `fprintf` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"w\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    int x = 123;\n    int n = fprintf(fp, \"The number is %d\\n\", x);\n    if (n < 0) {\n        printf(\"Failed to write to file\\n\");\n        return 1;\n    }\n    printf(\"Wrote %d characters\\n\", n);\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数以写入模式打开一个文件。然后，它使用 `fprintf` 函数将一个格式化的字符串写入到文件。如果写入失败，程序打印一条错误消息并返回 `1`。然后，程序使用 `printf` 函数打印写入的字符数量。最后，程序使用 `fclose` 函数关闭文件。\n\n\n\n# feof函数\n\n在C语言中，`feof` 函数用于检查一个打开的文件是否到达了文件末尾。这个函数的参数是一个 `FILE *` 指针，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。如果到达了文件末尾，`feof` 函数返回非零值；如果没有到达文件末尾，它返回零。\n其函数原型为：`int feof(FILE *fp);`\n\n以下是一个使用 `feof` 的例子：\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    int c;\n    while ((c= fgetc(fp)) != EOF) {\n        putchar(c);\n    }\n    if (!feof(fp)) {\n        printf(\"Failed to read to end of file\\n\");\n        return 1;\n    }\n    if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数打开一个文件。然后，它使用 `fgetc` 函数在一个循环中读取文件的每一个字符，直到到达文件末尾。如果没有到达文件末尾，程序打印一条错误消息并返回 `1`。最后，程序使用 `fclose` 函数关闭文件。\n\n\n\n# 文件定位读写\n\n\n文件的读写位置指针最小值为0，最大值为文件的长度。文件的位置指针位于文件首部，随着数据的读出和写入，文件的位置指针会发生移动。\n\n## rewind函数\n\n在C语言中，`rewind` 函数用于将一个打开的文件的位置指针重置到文件的开始。这个函数需要一个 `FILE *` 指针作为参数，这个指针必须是由 `fopen`、`freopen` 或 `tmpfile` 函数返回的。其函数原型如下：`void rewind(FILE *fp);`\n\n`rewind` 函数没有返回值。如果你需要检查操作是否成功，可以使用 `ferror` 或 `feof` 函数。\n\n以下是一个使用 `rewind` 的例子：\n\n```c\n#include <stdio.h>\nint main() {\n    FILE *fp = fopen(\"file.txt\", \"r+\");\n    if (fp == NULL) {\n        printf(\"Failed to open file\\n\");\n        return 1;\n    }\n    fputs(\"Hello, world!\", fp);\n    rewind(fp);\n    char buffer[100];\n    fgets(buffer, sizeof(buffer), fp);\n    printf(\"Read from file: %s\\n\", buffer);\n if (fclose(fp) == EOF) {\n        printf(\"Failed to close file\\n\");\n        return 1;\n    }\n    return 0;\n}\n```\n\n在这个例子中，程序首先使用 `fopen` 函数以读写模式打开一个文件。然后，它使用 `fputs` 函数向文件中写入一个字符串。然后，它使用 `rewind` 函数将文件的位置指针重置到文件的开始。然后，它使用 `fgets` 函数从文件中读取一个字符串，并使用 `printf` 函数打印这个字符串。最后，程序使用 `fclose` 函数关闭文件。\n\n---\n## fseek函数\n\n 在c语言中，fseek函数用于将一个打开的文件的读写位置指针移动到特定的位置。\n其函数原型为：`int fseek(FILE *fp, long offset, int whence)`，即将位置指针移动到距离whence的offset字节处。offset取值为正，即表示将文件位置指针移动到whence后面；为负，则表示将指针移动到whence的前面。\n\n**注意：** fseek函数一般用于二进制文件。在文本文件中由于要进行转换，故往往计算的位置会出现错误。\n\nwhence取值如下：\n\n| whence的常量值 | 对应数值 | 含义          |\n| ---------- | ---- | ----------- |\n| SEEK_SET   | 0    | 文件开始处       |\n| SEEK_CUR   | 1    | 文件位置指针的当前位置 |\n| SEEK_END   | 2    | 文件的末尾       |\n\n若操作成功，则fseek函数返回0；否则返回非0值。\n\n以下是fseek函数使用的一个例子：\n```c\nFILE *fp=fopen(\"test.txt\",\"r+\");\n//将文件位置指针从文件开头向右移动10字节 ===》等价于：fseek(fp,10L,SEEK_SET);\nfseek(fp,10L,0);\n// 将文件位置指针从当前位置向左移动10字节 ===》等价于：fseek(fp,-10L,SEEK_CUR);\nfseek(fp,-10L,1);\n// 将文件位置指针从文件末尾向左移动10字节 ===》等价于：fseek(fp,-10L,SEEK_END);\nfseek(fp,-10L,2);\n```\n\n---\n\n## ftell函数\n\n在c语言中，ftell函数返回打开文件的当前读写位置指针的值。(用相对于文件开头的位移量表示)。其函数原型为：`long ftell(FILE *fp);` 如果操作成功，则返回当前位置指针的值；如果出错，则返回-1L。\n\n\n"}]